# 疎通確認用のテストファイル(削除可)

作成: 2026-08-11
目的: 予想記録の自動push経路(deploy key + SSH)が動くことの確認のみ。

**このファイルは予想記録ではありません。削除して構いません。**

本番の予想記録は 2026-08-15 の開催日から
`predictions/{YYYY}/{YYYY-MM-DD}.jsonl` に、
1レースごとに発走の約35分前へpushされます。
